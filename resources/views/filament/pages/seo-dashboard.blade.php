<x-filament-panels::page>
    {{--
        Header widgets are auto-rendered by <x-filament-panels::page> from
        SeoDashboard::getHeaderWidgets(). Do NOT render them explicitly via
        <x-filament-widgets::widgets> here — the layout already does that
        and explicit rendering duplicates every widget on the page.
    --}}
</x-filament-panels::page>
