# Weekly Summary — 2026-09-20 → 2026-09-26

**Headline:** A routine weekly picks run turned into a full pw2d maintenance pass and exposed how much the project had
been trusting green lights: the nightly GA4 pull had stored 42–69% of real sessions since April, pw2d's Search Console
feed had been silently dropping a day, and a "dead" PostHog key had simply been tested against the wrong region. Three
pw2d `/best/` pages were rebuilt from a cleaned pool, and the site → store click is measured for the first time.

## By session (newest first)

- **2026-09-21 — a HEALTHY status hid two data bugs; three pw2d pages rebuilt; store clicks now measured** → [2026-09-21-healthy-status-hid-two-data-bugs-three-pages-rebuilt.md](2026-09-21-healthy-status-hid-two-data-bugs-three-pages-rebuilt.md) —
  Spec 040 (GA4 outbound clicks + 3-day GA4 window, `86731d6`) and the GSC top-query overflow fix (`a667f2d`) both deployed
  and backfilled; ergonomic keyboards, gaming chat headsets and mechanical gaming keyboards pages rebuilt and FRESH; 23
  non-keyboards, kits and duplicates hidden; `/summary` added and `todo.md` split from 1,539 lines to 35.

## Net outcomes

- **Shipped to prod (`a667f2d`):** store clicks stored nightly per page; GA4 re-pulled over a 3-day window; GSC write can
  no longer be failed by one over-long query. Suite 837 → 858.
- **Measured:** store clicks per 28 days — c2d 5, pw2d 1 (c2d 21 over 56 days, 9 from compare pages). Page-one
  impressions doubled on both tenants in the same week (Google-side, unattributed); clicks flat at 17 and 26.
- **Content:** pw2d 3 of 5 `/best/` pages FRESH; their three categories HEALTHY. c2d untouched: 5 of 6 pages STALE.
- **Falsified:** "PostHog key is dead" (wrong region) · "pw2d's decline is genuine" (reversed 27.6 → 9.9) ·
  "command files are guarded against edits" (stale note). Weighted position retired as the headline SEO number.
- **Method:** owner-reviewed drafts in `docs/drafts/`; page saves go through a selection guard and a price guard with
  a JSON backup; every prod write dumped first to `/root/backups/`. Task files are now todo / backlog / archive; `/architect` boots from this file and `/summary` closes into it.

## Carry-forward (open)

- **Content:** pw2d podcast-studio-mics (top-up → rescan → rebuild; last swept 08-14) · pw2d lavalier (rebuild only) ·
  c2d four price-only patches · c2d cold-brew full path.
- **Specs:** import quality — wrong product types accepted, names not "Brand Model", same-model pick guard unreliable,
  picks do not require a health check · product-page content depth (Spec 028 candidate) · surface nightly pull failures.
- **SEO, for ~09-28:** do page-one impressions hold and do clicks move · first store-click trend · what c2d's 5× traffic
  jump from 09-17 is · whether pw2d's ~1,500 GA4 sessions of 08-24 → 09-06 were bots · the 0-click headsets snippet at
  position 6.4 · first engagement read including c2d not before ~10-05.
- **Owner:** 22 leftover files in prod `/tmp` from 08-28 → 29, including two page backups a reboot would erase.
- **Cadence:** weekly picks run (both tenants) + `/seo-status` due **~2026-09-28**. Monthly sweep **overdue on all six c2d
  categories** (last 08-16 → 08-21) and on pw2d podcast-studio-mics (08-14); lavalier next due ~09-30. Quarterly
  discovery not due before November. Pages still STALE: pw2d podcast-studio-mics, lavalier; c2d cold-brew-makers,
  gooseneck-kettles, manual-coffee-grinders, pour-over, super-automatic.
